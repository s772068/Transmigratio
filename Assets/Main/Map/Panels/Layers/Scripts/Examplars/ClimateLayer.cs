public struct ClimateLayer : ILayer {
    public void Show(SettingsController settings, WmskController wmsk, MapController map, string layer) {
        //UnityEngine.Color color;
        //for (int i = 0; i < map.data.Regions.Length; ++i) {
        //    color = settings.Theme.GetEcologyColor(layerIndex, map.data.Regions[i].Ecology[layerIndex].MaxIndex);
        //    map.data.Regions[i].Color = color;
        //    wmsk.RegionPainting(i, color);
        //}
    }
}
